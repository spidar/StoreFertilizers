@{
    ViewData["Title"] = "สร้างใบกำกับสินค้า";
}
@if (ViewData.ContainsKey("isTax") && (bool)ViewData["isTax"])
{
    await Html.RenderPartialAsync("EditOrCreateTaxInvoicesPartialView");
}
else
{
    await Html.RenderPartialAsync("EditOrCreateInvoicesPartialView");
}
